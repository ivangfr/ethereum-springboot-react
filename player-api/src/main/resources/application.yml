spring:
  application:
    name: player-api
server:
  port: 8081

ethereum:
  contract:
    soccermanager:
      address:

management:
  endpoints:
    web:
      exposure.include: beans, env, health, info, metrics, mappings
  endpoint:
    health:
      show-details: always

springdoc:
  show-actuator: true
  swagger-ui:
    groups-order: DESC